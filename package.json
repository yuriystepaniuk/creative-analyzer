{
  "name": "creative-analyzer-root",
  "version": "1.0.0",
  "description": "Ad creative analyzer — monorepo root",
  "private": true,
  "scripts": {
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "deploy:backend": "npm run deploy --prefix backend",
    "deploy:frontend": "npm run build --prefix frontend && npx wrangler pages deploy frontend/dist --project-name creative-analyzer-frontend",
    "typecheck": "npm run typecheck --prefix backend"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yuriystepaniuk/creative-analyzer.git"
  },
  "license": "ISC"
}
